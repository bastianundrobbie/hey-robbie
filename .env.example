# Robbie server — API keys. Copy to .env and fill in; never commit .env.
#
#   cp .env.example .env

# LLM — Anthropic API key (console.anthropic.com). Pay-per-use.
ANTHROPIC_API_KEY=

# Speech-to-text — Deepgram (console.deepgram.com), Nova-3 / Flux.
DEEPGRAM_API_KEY=

# Text-to-speech — Cartesia (play.cartesia.ai), Sonic.
CARTESIA_API_KEY=
